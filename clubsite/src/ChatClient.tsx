import React,{ReactNode} from "react"

interface Props
{
    chatRows:ReactNode[];
}

export const ChatClient=(props:Props)=>
{
    return (
        <div style={{height:"90%"}}>
            n
        </div>
    );
};